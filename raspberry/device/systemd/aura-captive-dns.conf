# DNS captif du portail Aura-Config : toute résolution → 10.42.0.1 pendant que
# le hotspot NetworkManager (mode shared) est actif. C'est ce qui fait que la
# page de configuration S'OUVRE TOUTE SEULE sur le téléphone (détection captive
# Android/iOS). Sans effet hors mode hotspot.
# → à copier dans /etc/NetworkManager/dnsmasq-shared.d/
address=/#/10.42.0.1
